{
  "name": "@spirobel/monero-wallet-api",
  "license": "MIT",
  "module": "dist/api.js",
  "type": "module",
  "main": "dist/api.js",
  "types": "dist/api.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "publish": "bun run build && bun run inlinesum && npm publish",
    "inlinewasm": "bun build templates/wasm/wasmFile.ts --target=browser --outdir dist/wasm-processing",
    "sumwasm": "cat dist/wasm-processing/wasmFile.js | sha256sum > checksum.txt",
    "inlinesumwasm": "bun run inlinewasm && bun run sumwasm",
    "build": "tsc",
    "clean": "rm -rf ./dist"
  },
  "files": ["dist"],
  "version": "0.2.0",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "3.24.1"
  }
}